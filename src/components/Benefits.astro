---
const benefits = [
  {
    title: 'Atención Personalizada',
    description: 'Analizamos tus necesidades específicas para ofrecerte el seguro que mejor se adapte a ti.',
    icon: 'fa-user-check'
  },
  {
    title: 'Respuesta Rápida',
    description: 'Asistencia inmediata en caso de siniestro, estamos disponibles 24/7.',
    icon: 'fa-bolt'
  },
  {
    title: 'Mejores Precios',
    description: 'Comparamos entre múltiples aseguradoras para ofrecerte la mejor relación calidad-precio.',
    icon: 'fa-tags'
  },
  {
    title: 'Trámites Sencillos',
    description: 'Nos encargamos de toda la documentación para que no tengas que preocuparte por nada.',
    icon: 'fa-file-signature'
  }
];
---

<section id="beneficios" class="section benefits-section">
  <div class="container">
    <div class="section-title text-center">
      <h2>¿Por qué elegirnos?</h2>
      <p>Descubre las ventajas de contar con nuestros servicios</p>
    </div>
    
    <div class="benefits-grid">
      {benefits.map(benefit => (
        <div class="benefit-card">
          <div class="benefit-icon">
            <i class={`fas ${benefit.icon}`}></i>
          </div>
          <div class="benefit-content">
            <h3>{benefit.title}</h3>
            <p>{benefit.description}</p>
          </div>
        </div>
      ))}
    </div>
    
    <div class="cta-container text-center">
      <h3>¿Listo para proteger lo que más valoras?</h3>
      <a href="#contacto" class="btn btn-primary">Solicita tu cotización gratis</a>
    </div>
  </div>
</section>

<style>
  .benefits-section {
    background-color: #fff;
  }
  .benefits-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
    margin-bottom: 4rem;
  }
  .benefit-card {
    display: flex;
    align-items: flex-start;
    gap: 1.5rem;
  }
  .benefit-icon {
    background-color: rgba(var(--primary), 0.1);
    color: rgb(var(--primary));
    width: 60px;
    height: 60px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    flex-shrink: 0;
  }
  .benefit-content h3 {
    margin-bottom: 0.5rem;
    font-size: 1.3rem;
  }
  .cta-container {
    background-color: rgba(var(--primary), 0.05);
    padding: 3rem;
    border-radius: 8px;
    margin-top: 2rem;
  }
  .cta-container h3 {
    margin-bottom: 1.5rem;
    font-size: 1.8rem;
  }
  
  @media (max-width: 768px) {
    .benefits-grid {
      grid-template-columns: 1fr;
    }
    .cta-container {
      padding: 2rem;
    }
  }
</style>
